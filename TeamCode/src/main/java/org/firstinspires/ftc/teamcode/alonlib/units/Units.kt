package org.firstinspires.ftc.teamcode.alonlib.units

/** 2π radians, 360 degrees. */
typealias Rotations = Double

/**
 * In range [[-1.0, 1.0]].
 *
 * Mostly used to set motor output.
 */
typealias PercentOutput = Double

/** Voltage in volts. */
typealias Volts = Double

/** Current in Amps (Ampere). */
typealias Amps = Int

/** Time in seconds. */
typealias Seconds = Double

typealias Mills = Long