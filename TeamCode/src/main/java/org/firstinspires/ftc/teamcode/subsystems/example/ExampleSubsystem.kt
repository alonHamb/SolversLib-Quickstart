package org.firstinspires.ftc.teamcode.subsystems.example

import com.qualcomm.robotcore.hardware.HardwareMap
import com.seattlesolvers.solverslib.command.SubsystemBase
import org.firstinspires.ftc.robotcore.external.Telemetry

class ExampleSubsystem(hardwareMap: HardwareMap, var telemetry: Telemetry) : SubsystemBase() {
    // --- hardware initialization and configuration ---
    /*
     *  each hardware device has its own value variable
     *
     *  val hardwareDevice = DeviceClass()
     */

    // --- state getters ---
    /*
     * all the getters for any state of the subsystem
     *
     * val state get() = stateConditions()
     */

    //  --- subsystemFunctions --
    /*
     * all functions needed to use this subsystem
     */

    // --- states class ---
    /**
     *  a class containing all of the states you want for this subsystem
     */

}